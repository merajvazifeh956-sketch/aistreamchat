[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  restartPolicyType = "always"

[deploy.ports]
  [deploy.ports.2053]
    protocol = "tcp"
  [deploy.ports.443]
    protocol = "tcp"
